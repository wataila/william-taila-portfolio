name: Keep Streamlit App Awake

on:
  schedule:
    - cron: "*/230 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: curl -L https://wataila-william-taila-portfolio-app-oxm8vl.streamlit.app/ || true
